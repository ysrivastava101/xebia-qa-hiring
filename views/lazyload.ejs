<% include includes/header.ejs %>

<section class="containerjumbotron">
	<div class="container">
		<h4 class="text-center">Lazyload</h4>
		<div class="form-group">
			<label for="country">Select Country</label>
			<select class="form-control" id="country" onchange="getCities(this.value)">
				<option value="" selected>Select a Country</option>
				<% for(var i=0;i<countries.length;i++) {%>
					<option value="<%= countries[i] %>"><%= countries[i] %></option>
				<% } %>
			</select>
		</div>
		<div class="form-group">
			<div id="cities">
				<table class="table table-striped">
			    <tbody></tbody>
				</table>
			</div>
		</div>
	</div>
	<button type="button" name="button" class="btn btn-lg continueBtn xebiaTheme" onclick="next()">Continue</button>
</section>
<script type="text/javascript">
	var getCities = function(country) {
		console.log(country);
	}
</script>
<% include includes/footer.ejs %>
